{
  "name": "vignette",
  "private": true,
  "version": "1.0.0",
  "description": "A platform for creating stylized title cards from movies, series, and anime",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=22.0.0"
  }
}
